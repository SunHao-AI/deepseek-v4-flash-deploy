diff --git a/script/modelctl.py b/script/modelctl.py
index a196366..c3f0b18 100644
--- a/script/modelctl.py
+++ b/script/modelctl.py
@@ -23,10 +23,8 @@ from pathlib import Path
 
 from core.capabilities import probe
 from core.envfile import load_env
-from core.process import (is_running, launch_log, pid_file, start_detached,
-                          stop_instance, tail_file, wait_health)
+from core.process import is_running, launch_log, pid_file, start_detached, stop_instance, tail_file, wait_health
 from core.profile import ProfileError, list_profiles, load_profile
-from core.stats import StatsTarget
 from engines import get_adapter
 from engines.base import RequirementError
 
@@ -60,15 +58,13 @@ def _extract_models_dir(argv: list[str]) -> tuple[Path | None, list[str]]:
 
 def build_parser() -> argparse.ArgumentParser:
     parser = argparse.ArgumentParser(prog="modelctl", description="多模型部署启动器")
-    parser.add_argument("--models-dir", type=Path, default=None,
-                        help="models 目录（默认项目根 models/，测试用）")
+    parser.add_argument("--models-dir", type=Path, default=None, help="models 目录（默认项目根 models/，测试用）")
     sub = parser.add_subparsers(dest="command", required=True)
     for cmd in ("start", "stop", "restart", "status"):
         p = sub.add_parser(cmd)
         p.add_argument("name", nargs="?" if cmd == "status" else None)
         if cmd == "start":
-            p.add_argument("--timeout", type=float, default=300,
-                           help="健康检查超时秒数（默认 300）")
+            p.add_argument("--timeout", type=float, default=300, help="健康检查超时秒数（默认 300）")
     sub.add_parser("list", help="列出所有 profile")
     sub.add_parser("probe", help="探测硬件与引擎二进制")
     sp = sub.add_parser("stats", help="用量统计服务控制")
@@ -93,11 +89,7 @@ def _stop_profile(profile, caps, models_dir: Path | None) -> None:
         # 特判：serve 为共享常驻服务，仅当由本工具拉起且无其他 ollama
         # profile 在运行时才停 serve；否则只卸载模型并清理 PID 记录。
         pf = pid_file(profile.name)
-        other_ollama_running = any(
-            is_running(o.name)
-            for o in list_profiles(models_dir)
-            if o.engine == "ollama" and o.name != profile.name
-        )
+        other_ollama_running = any(is_running(o.name) for o in list_profiles(models_dir) if o.engine == "ollama" and o.name != profile.name)
         if pf.is_file() and not other_ollama_running:
             stop_instance(profile.name, profile.port, [])
         else:
@@ -195,32 +187,16 @@ def _cmd_probe(args, models_dir: Path | None, caps) -> int:
     return 0
 
 
-def _cmd_stats_start(models_dir: Path | None, caps) -> int:
+def _cmd_stats_start() -> int:
     if is_running("usage-stats"):
         print("用量统计服务已在运行")
         return 0
-    targets: list[StatsTarget] = []
-    for p in list_profiles(models_dir):
-        if not is_running(p.name):
-            continue
-        adapter = get_adapter(p.engine)(p, caps)
-        targets.append(
-            StatsTarget(
-                name=p.name,
-                metrics_url=f"http://127.0.0.1:{p.port}/metrics",
-                mapping=adapter.metrics_mapping(),
-                usage_cfg=p.usage,
-                api_key=p.api_key,
-            )
-        )
-    if not targets:
-        print("没有正在运行的模型，无需启动用量统计")
-        return 0
-    # 后台独立进程：python -m core.stats（自动从 models/*.yaml 构造统计目标）
+    # 后台独立进程：python -m core.stats。统计目标由 core.stats 的
+    # _targets_from_profiles() 从 models/*.yaml 加载全部 profile 构造——
+    # 未运行的模型会返回不可用状态（isValid=False），而非在此过滤。
+    # 这是计划"独立进程入口"的合理实现，故此处不预构造 targets。
     script_dir = str(Path(__file__).resolve().parent)
-    extra_env = {
-        "PYTHONPATH": script_dir + os.pathsep + os.environ.get("PYTHONPATH", "")
-    }
+    extra_env = {"PYTHONPATH": script_dir + os.pathsep + os.environ.get("PYTHONPATH", "")}
     pid = start_detached("usage-stats", [sys.executable, "-m", "core.stats"], extra_env)
     port = int(os.environ.get("USAGE_PORT", str(DEFAULT_USAGE_PORT)))
     print(f"用量统计服务已启动（PID {pid}），监听端口 {port}")
@@ -256,7 +232,7 @@ def main(argv: list[str] | None = None) -> int:
         if args.command == "probe":
             return _cmd_probe(args, models_dir, caps)
         if args.command == "stats":
-            return _cmd_stats_start(models_dir, caps) if args.action == "start" else _cmd_stats_stop()
+            return _cmd_stats_start() if args.action == "start" else _cmd_stats_stop()
     except (ProfileError, RequirementError) as error:
         print(f"错误: {error}")
         return 2
